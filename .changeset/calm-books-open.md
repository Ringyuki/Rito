---
'@ritojs/core': patch
'@ritojs/kit': patch
'@ritojs/react': patch
---

Accept legacy HTML-style void elements such as `<br>` and `<img>` in EPUB XHTML
while preserving strict XML failures for other malformed markup.
