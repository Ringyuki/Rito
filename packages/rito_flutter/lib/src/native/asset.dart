/// Package namespace used by the Native Assets protocol.
const String ritoNativeAssetPackage = 'rito_flutter';

/// The logical Native Asset name emitted by `hook/build.dart`.
const String ritoNativeAssetName = 'src/native/bindings.dart';

/// The stable package URI used by every `@Native` Rito ABI binding.
const String ritoNativeAssetId =
    'package:$ritoNativeAssetPackage/$ritoNativeAssetName';
